@{ ViewData["Title"] = "Access Denied"; }

<div style="max-width:460px;margin:3rem auto;">
  <div class="auth-form-card text-center">
    <i class="bi bi-shield-lock text-warning" style="font-size:3rem;"></i>
    <h2 class="fw-bold mt-3 mb-1">Access denied</h2>
    <p class="text-muted small mb-4">You do not have permission to view this page.</p>
    <a class="btn btn-primary w-100 py-2" href="@Url.Action("Index", "Home")"><i class="bi bi-house"></i> Back to home</a>
  </div>
</div>
