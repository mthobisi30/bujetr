@model CreateAccountViewModel
@{ ViewData["Title"] = "Add account"; var sym = System.Globalization.NumberFormatInfo.CurrentInfo.CurrencySymbol; }

<div class="page-header">
    <h2><i class="bi bi-bank text-primary"></i> Add account</h2>
    <a class="btn btn-sm btn-outline-secondary" href="@Url.Action("Index")"><i class="bi bi-arrow-left"></i> Back</a>
</div>

<div class="card"><div class="card-body">
<form asp-action="Create" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row g-3">
        <div class="col-md-5">
            <label asp-for="Name" class="form-label"></label>
            <input asp-for="Name" class="form-control" placeholder="e.g. Capitec cheque" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="col-md-4">
            <label asp-for="AccountType" class="form-label">Type</label>
            <select asp-for="AccountType" class="form-select">
                @foreach (AccountType t in Enum.GetValues<AccountType>()) { <option value="@t">@(((Enum)t).Humanize())</option> }
            </select>
        </div>
        <div class="col-md-3">
            <label asp-for="StartingBalance" class="form-label">Starting balance</label>
            <div class="input-group"><span class="input-group-text">@sym</span><input asp-for="StartingBalance" class="form-control" type="number" step="0.01" /></div>
        </div>
    </div>
    <div class="mt-4"><button class="btn btn-primary"><i class="bi bi-check-lg"></i> Add</button>
        <a class="btn btn-link" href="@Url.Action("Index")">Cancel</a></div>
</form>
</div></div>
@section Scripts { @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); } }
